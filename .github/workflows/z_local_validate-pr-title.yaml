name: validate-pr-title
on:
  pull_request_target:
    types:
      - opened
      - synchronize
      - edited
      - reopened
permissions: read-all

jobs:
  validate-pr-title:
    name: validate-pr-title
    permissions:
      pull-requests: read
    uses: ./.github/workflows/validate-pr-title.yaml
    secrets: inherit
