name: validate-pr-title
on:
  pull_request_target:
    types:
      - opened
      - synchronize
      - edited
      - reopened
permissions:
  contents: read

jobs:
  validate-pr-title:
    name: validate-pr-title
    permissions:
      contents: read
      pull-requests: read
    uses: ./.github/workflows/validate-pr-title.yaml
    secrets: inherit
