name: semver-release
on:
  push:
    branches:
      - main
permissions:
  contents: read

jobs:
  semver-release:
    name: semver-release
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    uses: ./.github/workflows/semver-release.yaml
    secrets: inherit
